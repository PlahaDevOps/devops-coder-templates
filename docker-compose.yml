# Secrets come from .env (gitignored). Do not put real tokens in git.
# CODER_ACCESS_URL is HTTP localhost below so dashboard + *.localhost workspace apps share one origin (no https:// mismatch). For GitHub OAuth, register callbacks on http://localhost:3000. ngrok remains optional for remote access to the tunnel only.

services:
  ngrok:
    image: ngrok/ngrok:latest
    container_name: ngrok
    restart: unless-stopped
    # ngrok v3: use --url (not deprecated --domain). Hostname-only NGROK_DOMAIN → full https URL.
    # Note: free-tier browser warning is decided at ngrok’s edge; you cannot fix CLI/API clients by
    # adding headers only on the tunnel → Coder hop — see README “ngrok free tier”.
    command:
      - http
      - --url=https://${NGROK_DOMAIN}
      - coder:3000
    environment:
      NGROK_AUTHTOKEN: "${NGROK_AUTH_TOKEN}"
    ports:
      - "4040:4040"

  coder:
    image: ghcr.io/coder/coder:v2.31.9
    container_name: coder
    restart: unless-stopped
    # Docker: host socket mounted below. (docker-dev Terraform still uses tcp://host.docker.internal:2375 — keep TCP enabled in Docker Desktop.)
    privileged: true
    depends_on:
      - ngrok
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      # Must match browser URL (http only here — no https://) so embedded apps / Tasks iframes resolve under *.localhost:3000.
      CODER_ACCESS_URL: "http://localhost:3000"
      # Hostname pattern only — no scheme (Coder rejects http:// here).
      CODER_WILDCARD_ACCESS_URL: "*.localhost:3000"
      CODER_HTTP_ADDRESS: "0.0.0.0:3000"
      # GitHub OAuth app "Coder Local" — sign-in (callback …/api/v2/users/oauth2/github/callback)
      CODER_OAUTH2_GITHUB_CLIENT_ID: "${GITHUB_CLIENT_ID}"
      CODER_OAUTH2_GITHUB_CLIENT_SECRET: "${GITHUB_CLIENT_SECRET}"
      CODER_OAUTH2_GITHUB_ALLOW_SIGNUPS: "true"
      CODER_OAUTH2_GITHUB_ALLOW_EVERYONE: "true"
      # GitHub OAuth app "Coder Local External" — git/external auth (callback …/external-auth/github/callback)
      CODER_EXTERNAL_AUTH_0_ID: "github"
      CODER_EXTERNAL_AUTH_0_TYPE: "github"
      CODER_EXTERNAL_AUTH_0_CLIENT_ID: "${GITHUB_EXTERNAL_CLIENT_ID}"
      CODER_EXTERNAL_AUTH_0_CLIENT_SECRET: "${GITHUB_EXTERNAL_CLIENT_SECRET}"
      CODER_EXTERNAL_AUTH_0_DISPLAY_NAME: "GitHub"
      # Git + org + user SSH keys on GitHub (github-upload-public-key module when enabled)
      CODER_EXTERNAL_AUTH_0_SCOPES: "repo read:org write:public_key"
      # Optional: passed through if you use server-side features expecting this var; k8s-dev workspaces use the K8s secret on the pod.
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./coder-data:/home/coder/.config
      - C:/Users/admin/.kube:/home/coder/.kube:ro
