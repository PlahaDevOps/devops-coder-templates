.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# Commit .terraform.lock.hcl for reproducible CI (providers pinned).

# Secrets: use parent Coder/.env (see README).
.env

# Optional local Docker Compose overrides
docker-compose.override.yml
