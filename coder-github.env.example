# Copy to coder-github.env (gitignored) and set both values.
#
# GitHub OAuth App (e.g. Developer settings → OAuth Apps → your app):
#   Authorization callback URL MUST be:
#   http://localhost:3000/api/v2/users/oauth2/github/callback
#
# "Coder Local" (PlahaDevOps) example Client ID — use yours if different:
CODER_OAUTH2_GITHUB_CLIENT_ID=Ov23liLlqZ88DveG0Lmu
CODER_OAUTH2_GITHUB_CLIENT_SECRET=YOUR_CLIENT_SECRET
